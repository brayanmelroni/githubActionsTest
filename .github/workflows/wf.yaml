name: Shell Commands
on: [push]
jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
          - name: echo a String
            run: eccho "Echo from Ubuntu"
          - name: multiline script
            run: |
              node -v
              npm -v  
